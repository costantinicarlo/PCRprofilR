% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcr_public_api.R
\name{as_pcr_peaks}
\alias{as_pcr_peaks}
\title{Normalize peak data into the PCRprofilR peak schema}
\usage{
as_pcr_peaks(dat, mapping = NULL, allow_qc_issues = FALSE)
}
\arguments{
\item{dat}{A data frame or existing \code{pcr_peaks} object. Raw data frames may
use supported legacy column names such as \code{RunID}, \code{PlateID}, \code{WellID},
\code{SampleID}, \code{PeakID}, \code{Size}, \code{Conc}, \code{RawFile}, and \code{Instrument}.}

\item{mapping}{Optional named character vector mapping canonical column names
to source column names.}

\item{allow_qc_issues}{Logical scalar. If \code{FALSE}, canonical validation is
strict. If \code{TRUE}, selected QC-able issues, currently missing or malformed
well identifiers, are allowed through for later QC review.}
}
\value{
A tibble-like \code{pcr_peaks} object.
}
\description{
\code{as_pcr_peaks()} is the entry point for turning raw instrument-like peak
tables into the canonical \code{pcr_peaks} object used by the deterministic
interpretation workflow.
}
\details{
If \code{dat} already has the canonical peak columns, it is validated and returned
as a \code{pcr_peaks} object. Otherwise, source columns are normalized with
\code{normalize_pcr_peaks()}, using either the default legacy column names or an
explicit \code{mapping}.
}
\seealso{
\code{\link[=validate_pcr_peaks]{validate_pcr_peaks()}}, \code{\link[=detect_pcr_peaks]{detect_pcr_peaks()}},
\code{vignette("PCRprofilR", package = "PCRprofilR")}
}
